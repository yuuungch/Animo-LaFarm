import java.util.ArrayList;

public class Play {
    public void playGame() {
        // Play play = new Play();
        // Seeds seedInfo = new Seeds(0);
        // Store store = new Store(8, seedInfo);
        // Player player = new Player(store);
        // Tile tile = new Tile(seedInfo, player.getExpData());
        // ArrayList<Tile> Land = new ArrayList<Tile>();
        // Farm farm = new Farm(Land, player, tile, play);
        // WindowStoreGui storeGui = new WindowStoreGui(seedInfo, Land, store);
        // Controller controller = new Controller(storeGui, store, player);
        // WindowFarmGui farmGui = new WindowFarmGui(farm);

        // farm.Menu();

        WindowAskGui askGui = new WindowAskGui();
    }
}
